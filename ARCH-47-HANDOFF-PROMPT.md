<!-- ARCH46-S1:handoff-47 -->
# ARCH-47 — ERP & System-of-Record Posting

You are continuing FlowPilot AI, a multi-tenant document-intelligence SaaS I build alone (FastAPI, PostgreSQL 16 + pgvector ≥ 0.8, Redis, MinIO, PaddleOCR, SentenceTransformers, SciPy, scikit-learn, pikepdf, pypdfium2, python-dateutil, React 19 + TanStack Query + Tailwind, Vite). Repository: https://github.com/sabarishwarne2001/flowpilot-ai (branch `main`). Zero new recurring external cost is a hard rule.

**Baseline.** The commit that contains ARCH-46 (Obligations & Temporal Intelligence). Alembic chain: `hm1_tier_price_per_key → arch41_step1_extraction_memory → arch42_step1_entity_graph → arch43_step1_case_intelligence → arch44_step1_table_intelligence → arch45_step1_corroboration → arch46_step1_obligations → arch40_step3_contract_ai_settings` (the last is the flag-gated lossy contract step; it must stay the ONLY file head and must never run unasked). Release head today: `arch46_step1_obligations`. Read `FlowPilot-AI-ARCH-41-to-50.md` and `ARCH-41` … `ARCH-46-FINAL-CERTIFICATION.md` (repository root) before anything else.

**Task: build ARCH-47 — ERP & System-of-Record Posting, capability `capability.erp_posting`, Business and Enterprise.** Specification: §4 "ARCH-47" of the roadmap. In brief:

- Exactly-once posting of APPROVED outcomes (a reconciled invoice, a confirmed table, a completed case) as canonical posting objects (vendor bill, purchase order, goods receipt, journal entry, payment reference), built from extracted values, ARCH-42 entity roots and ARCH-44 line items.
- A safe mapping language (no eval, no templates that execute): field paths, typed transforms (date formats, number formats, currency, lookups against stored tables), defaults and required-field checks, versioned per workspace and per target.
- Adapters: CSV/XLSX, EDI X12 810/850/856, UBL 2.1, Tally Prime XML, SFTP, generic REST/OData, with presets for QuickBooks Online, Zoho Books, Business Central, S/4HANA OData and NetSuite REST.
- An idempotency ledger (one posting per object and target, whatever retries happen), acknowledgement reconciliation (a posting is only DONE when the target says so), and a `erp.post` Flow Builder action plus a `posting.failed` trigger (both in the live conformance matrix).
- Gates: golden files for every format validated against their published schemas, mock servers for REST/OData/SFTP (no live ERP: those need customer sandboxes), exactly-once under concurrent retries, acknowledgement mismatch handling, mapping-language refusal of anything executable.

**Facts proven by the ARCH-41–46 builds (do not rediscover them):**

- Capability registration is SIX places plus gates: `app/core/entitlements.py` (constant, `CAPABILITY_KEYS`, an `Entitlement(...)` entry — without it `has_capability` raises), `app/api/capability_gate.*DISPLAY*NAMES`, `scripts/seed_quota_tiers.py` (BUSINESS_CAPABILITIES and ENTERPRISE_CAPABILITIES for a Business key), frontend `constants/capabilities.ts`, `constants/planFeatures.ts` — the typed label AND `PLAN_FEATURE_ORDER` — `verify_arch36.py GATED_PAGES`, and `verify_hardening_master.py` BUS and ENT. Then `python scripts/seed_quota_tiers.py --carry-forward`. On a fresh database seed the price book first (`scripts/seed_price_book.py`; `run_arch46.ps1 -SeedPriceBook`) or the tier seed fails.
- Gating is imperative: `capability_gate.require_capability(db, context=..., capability_key=..., operation=...)` → 402 `CAPABILITY_REQUIRED`, the second line of every route after the workspace check (`verify_arch46` W2 checks exactly that, and each route's role). In a rolled-back test transaction, patch `audit_service.record_independently`.
- `work_items` has NO `organization_id` (use `extraction_memory.gate.organization_of`); `work_items.extracted_entities` is `json`, not `jsonb`. Per-page OCR is in `work_items.extraction_metadata["pages"][i]`.
- **Building blocks to reuse:** ARCH-45 `corroboration/normalize.py` (typed values: money with currency, dates DMY/MDY, identifiers), `corroboration/segment.py` (clauses with evidence spans; ARCH-46 fixed a wrapped last line with no ascenders being split off as its own clause), `corroboration/loader._roots` (follow `entities.merged_into_id` to the root in one recursive CTE — post against the ROOT party), `tables.service.load` (ARCH-44 line items), `report.PdfWriter` (pikepdf, Helvetica metrics). ARCH-46 `obligations/temporal.py` (month ends, business days on STORED holiday calendars, RRULE, local dates in a workspace zone) and `obligations/holidays.py` — use them for any due date or value date a posting carries; `obligations/ical.py` is a small RFC 5545 writer.
- Outbound calls (ERP REST/OData/SFTP) are new egress: every target URL goes through the existing webhook preflight (`webhook_service._preflight_check_url`: no private or link-local addresses), credentials are stored encrypted with the configured `EMAIL_ENCRYPTION_KEYS` (`app.core.encryption`), and nothing is fetched at import time. `requests`/`httpx` are available; retries need jitter and a ceiling.
- `app.services` re-exports an `llm_service` INSTANCE; use `importlib.import_module("app.services.llm_service")` for the module. Posting must be deterministic; any model call is billed and needs the ARCH-33 acknowledgement pattern.
- A new job type is registered in three places or the fleet refuses to boot: the phase constant and `_HANDLERS` in `app/workers/handlers/__init__.py`, and a profile in `app/workers/profiles.py` (LIGHT for network and file work: it forbids torch/sentence_transformers/paddle). A harness that calls handlers directly must call `app.workers.handlers.register_all()` first; run a handler inside a rolled-back test transaction by patching `app.db.session.SessionLocal` with a context manager yielding the test session, WITHOUT a nested savepoint. Before a dry-run that calls `db.rollback()`, `db.commit()` in the test session first.
- Post-enrichment hook: `app/services/post_enrichment.dispatch_in_session` (ARCH-42 … ARCH-46 dispatches). Wrap anything that must never fail the dispatch in `with db.begin_nested():` and log. ARCH-46's obligation reading is enqueued with `available_at` 90 s later so ARCH-42 has resolved the parties first — do the same if postings need resolved parties.
- Claiming is weighted-fair by default (ARCH-43): never reintroduce `UPDATE … IN (LIMIT subquery)`.
- A `scripts/sweep_<arm>.py` must be dispatched in `deploy/bin/flowpilot-sweep` and scheduled in `deploy/cron.d/flowpilot-sweepers` (G14); keep the file's trailing newline. New scripts need the executable bit (the run script does `git update-index --chmod=+x`).
- Review hub kinds: EXTRACTION, ASSERTION, ANOMALY, MERGE (42), SPLIT (43), TABLE (44), CORROBORATION (45), OBLIGATION (46, reason OBLIGATION_UNCONFIRMED). A new kind (e.g. POSTING / POSTING_FAILED) touches `app/models/review.py` REVIEW_KINDS, the `ck_review_assignments_kind_known` CHECK (recreate it), the `review_queue_items` view (ARCH-46's `review_queue_view_v7()` = v6 + an OBLIGATION arm — append, load it, do not copy), `services/review/vocabulary.py`, `resolution._DISPATCH` + `ResolvePayload`, `schemas/review.py` (verdict field + validator), `api/v1/review.*allowed*kinds` (capability-gated) and `_payload`, `frontend/src/types/review.ts`, `ResolvePanel`, the hub TABS (and a bulk action if the verdict is kind-wide), and widen `verify_arch40` A6/H1, `verify_arch42` vocab, `verify_arch43` vocab, `verify_arch44` T2 vocab and console kinds, `verify_arch45` T2 vocab and W3 console kinds, `verify_arch46` T2 (each compares with the NEWEST hub migration).
- New automation triggers/actions: events in `app/core/automation_events.py` (INTERNAL and TRIGGER_NATIVE), `TriggerSpec`s in `app/services/automation/triggers.py`, rebuild `ck_outbox_events_visibility_vocabulary` in your migration (ARCH-46's `internal_after_46()` is the current list), emitters in `verify_arch37` EMITTERS + `later` set + count pins, widen counts in `verify_arch38` B2, `verify_arch40` T1, `verify_arch41` check_catalog, `verify_arch43` T8/M16, `verify_arch44` W1, `verify_arch45` W1 and `verify_arch46` W1, and `scripts/automation_conformance.EXPECTED_TRIGGERS` (21 today over 22 events). An ACTION (`erp.post`) is new ground since ARCH-37: read `flow_service` and the action registry before adding one. Emit with an idempotency key per object and outcome. Keep the live conformance matrix ALL CONFORMANT (`python scripts/automation_conformance.py --json <file>` — it COMMITS a dedicated organization; development database only).
- Unauthenticated routes go in `app/core/public_route_registry.PUBLIC_ROUTES` or the app refuses to boot. A credential in a path (ARCH-43 document requests, ARCH-46 calendar feeds) is stored only as a sha256 (`char(64)`, hex CHECK, UNIQUE), compared in constant time, answered identically for every refusal, and REDACTED from logs: `public_route_registry.redact_path` (every route whose path contains `{token}` is covered automatically), `logging_config.RedactSecretPaths` on the console handler (uvicorn's access log included) and the Caddyfile's `format filter` on `request>uri`. `scripts/verify_arch21.py` G8 flags every `/api/v1/public/*` route registered public (pre-existing since ARCH-43's document requests; ARCH-46's feed follows the same pattern) — decide deliberately before adding another.
- **Caddy:** `caddy:2-alpine` is Caddy 2.10, which refuses `on_demand_tls { interval … burst … }`; ARCH-46 removed them (the ask endpoint decides). A Caddy 2.10.2 binary from the GitHub release validates the Caddyfile in the sandbox (`caddy validate --adapter caddyfile`, with `APP_DOMAIN` and `ACME_CONTACT_EMAIL` set); `verify_arch46` L2 runs it when `$CADDY` is set.
- **One clock.** ARCH-46's service and API read `obligations.service.now()`; tests pin it. Anything date-dependent you add should take its clock from one injectable place, never `datetime.now()` scattered through routes (a gate asserting "today" otherwise breaks on the next calendar day).
- **Python naming trap found in ARCH-46:** a module that defines `def update(...)` (a person's edit) and also imports SQLAlchemy's `update` silently shadows it — every `db.execute(update(Model)…)` then calls the service function. Import SQLAlchemy's as `sa_update` (ARCH-46 does). Same for `delete`, `select`, `text`.
- Nullable request fields: a client may send `null` for "not given"; `int(spec.get("x", default))` then raises. Treat `None` as the default explicitly.
- `python-dateutil` and `tzdata` ARE pinned in `requirements.txt` (the ARCH-46 prompt said dateutil was only transitive; it was already pinned). `requirements.txt` is UTF-16LE with CRLF in the working tree; read it with `iconv -f UTF-16`.
- Console dates: an INSTANT is formatted with `utils/displayTime.formatTimestamp` / `formatTimestampDate` (the reader's profile zone and language); a CALENDAR date ("2026-02-28") with `formatCalendarDate` / `formatCalendarMonth` (never zone-shifted). `new Date(x).toLocale*String(` anywhere in `src` fails `scripts/verify_arch30_tranche3.py` H8 (ARCH-46 fixed the four files ARCH-42/43 left).
- Models leave constraints to migrations, but column types must match exactly (autogenerate comparison; ARCH-46 has zero drift). A model column named `text` shadows SQLAlchemy's `text()` inside the class body — import it as `sa_text`.
- Audit outcomes are `ALLOWED`/`DENIED`; reuse `AuditResourceType.WORKSPACE` with structured details; exports, credential creation and every posting are audited.
- Composite keys: `workspaces (id, organization_id)`, `work_items (id, workspace_id)`, `entities (id, workspace_id)`, `cases (id, workspace_id)`, `extracted_tables (id, workspace_id)`, `extraction_templates (id, workspace_id)`, `corroboration_runs (id, workspace_id)`, `obligations (id, workspace_id)`, `holiday_calendars (id, workspace_id)`, `workspace_members (user_id, workspace_id)`. PostgreSQL 16's `ON DELETE SET NULL (column)` clears one column of a composite FK (ARCH-46 uses it). A composite FK with CASCADE onto `work_items` deletes rows with the document; ARCH-46's `trg_work_items_unlink_obligations` shows how to keep a person's own rows (unlink before delete).
- New migrations insert BEFORE the contract step and re-parent it; one revision per milestone; `run_arch47.ps1` handles a database whose contract already ran (stamp beneath, upgrade, stamp back).
- Widen head pins in `verify_arch31, 31_step0, 34–46`, the chains in `verify_arch40` A8, `verify_arch41` E2, `verify_arch42` E2, `verify_arch43` T2, `verify_arch44` T2, `verify_arch45` T2, `verify_arch46` T2 (it pins `STEP3 == arch46`: widen it) and `verify_hardening_master`, each with an `ARCH47-S1:` sentinel, never replacing an earlier one. `verify_arch46` A1 runs `apply_arch46.py --check`, which reports files a later milestone changed as "superseded" (its `LATER_MILESTONE` regex matches `ARCH47-S1:`), so keep a sentinel in every file you touch.
- Known pre-existing failures (reproduce on the baseline before worrying): `verify_arch38` A1 (`apply_arch38.py` refuses `preset_service.py`, edited by ARCH-42); `scripts/verify_arch30_tranche1.py` 30T1-G4 (no `KNOWN_METERS` entry for any bundled capability); `scripts/verify_arch21.py` G8 (see above) and G16; `verify_hardening_master --db` D1/D5/D6/D7 and `verify_hardening_final --db` "Payment loop (Stripe)" without real gateway price ids and webhook secrets; `scripts/verify_arch10_step3.py` S1.6/S2.3, `scripts/verify_arch10_step9.py` G2.2/G5.3. **Never run `verify_arch10_step3.py` against a database you back up.**
- Mutation harnesses: `exec` a module's source only after registering it in `sys.modules`; build the anchor BEFORE the refusal check; **patch the binding the code actually uses**; record WHY each mutation was caught (`caught_by` in the evidence) and READ it. ARCH-46 found two mutations "caught" for the wrong reason: a rebuilt module raises its OWN copy of an exception class, which the application's `except` clauses no longer catch — `verify_arch46._share_exceptions` binds the original classes into every rebuilt copy. A gate that reads a file's text must be mutated through a changed COPY of the file (`_with_file`), not through a patched function (`inspect.getsource` reads the original from disk). A partial unique index can mask a broken cache — break the KEY, not the lookup.
- A mutation run can stop after the step it targets (`live_e2e(patches, until=...)`): ARCH-46's 21 database mutations then take minutes, not an hour.
- Test data with random UUIDs: never assert on `json.dumps` of a dict keyed by ids. Access an ORM instance's id BEFORE deleting its row in bulk (an expired, deleted instance raises `ObjectDeletedError`); raw `UPDATE`s need `db.expire_all()` before the ORM reads the row again.

**Sandbox (the build environment, not the product):** check `python3 --version` — if it is not 3.12, create a 3.12 venv. PostgreSQL may not survive between tool calls — start it at the top of every call that needs it. Ubuntu's pgvector is 0.6; build 0.8.1 from https://github.com/pgvector/pgvector (tag v0.8.1) after `apt-get install postgresql-server-dev-16`. Install `backend/requirements.txt` (convert from UTF-16 first) without torch/paddle/paddlex/sentence-transformers/transformers. Hugging Face downloads are refused by the sandbox proxy (403). GitHub release downloads work (the Caddy binary). Run `npm ci --ignore-scripts` in the foreground. Replay the full chain on an empty database (≈4 s). `.gitattributes` pins `eol=lf`, so prove CRLF by rewriting a clean copy's text files to CRLF. A `git worktree` of the baseline gives a clean tree for reproducing pre-existing failures. Long runs (the full certification with `--regression` takes several minutes) go in the background with the log polled.

**How to work:**
1. Clone, then audit the real code before writing anything. Correct anything in this prompt or the roadmap that the code contradicts, and say what you corrected.
2. Build in the clone; sentinels `ARCH47-S1:` (backend) / `ARCH47-S2:` (console).
3. Deliver `backend/apply_arch47.py` (generated from `git diff <baseline>` like `apply_arch46.py`: sha-guarded `new` and `edit` operations, atomic, `--check`/`--rollback`/`--list`), `backend/verify_arch47.py` (offline; `--db` end to end in a rolled-back transaction including HTTP 402/200 and mock ERP targets; `--mutate` where every mutation must be caught for the RIGHT reason; `--build`; `--regression` → `verify_arch46.py --db`), `run_arch47.ps1`, `ARCH-47-FINAL-CERTIFICATION.md`, and the ARCH-48 handoff prompt.
4. Prove apply idempotency and rollback on clean LF and CRLF trees of the baseline, and run `verify_arch47.py --db --mutate --build --regression` before certifying. Report only what actually passed, and say plainly what could not be run in the sandbox.
