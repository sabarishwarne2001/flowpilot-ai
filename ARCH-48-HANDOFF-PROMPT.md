<!-- ARCH47-S1:handoff-48 -->
# ARCH-48 — Real-Time Collaborative Review & Live Presence

You are continuing FlowPilot AI, a multi-tenant document-intelligence SaaS I build alone (FastAPI, PostgreSQL 16 + pgvector ≥ 0.8, Redis, MinIO, PaddleOCR, SentenceTransformers, SciPy, scikit-learn, pikepdf, pypdfium2, python-dateutil, paramiko, React 19 + TanStack Query + Tailwind, Vite). Repository: https://github.com/sabarishwarne2001/flowpilot-ai (branch `main`). Zero new recurring external cost is a hard rule.

**Baseline.** The commit that contains ARCH-47 (ERP & System-of-Record Posting). Alembic chain: `hm1_tier_price_per_key → arch41_step1_extraction_memory → arch42_step1_entity_graph → arch43_step1_case_intelligence → arch44_step1_table_intelligence → arch45_step1_corroboration → arch46_step1_obligations → arch47_step1_erp_posting → arch40_step3_contract_ai_settings` (the last is the flag-gated lossy contract step; it must stay the ONLY file head and must never run unasked). Release head today: `arch47_step1_erp_posting`. Read `FlowPilot-AI-ARCH-41-to-50.md` and `ARCH-41` … `ARCH-47-FINAL-CERTIFICATION.md` (repository root) before anything else.

**Task: build ARCH-48 — Real-Time Collaborative Review & Live Presence, capability `capability.collaborative_review`, Enterprise only.** Specification: §4 "ARCH-48" of the roadmap. In brief:

- FastAPI WebSockets authenticated with the existing session (the cookie/bearer the REST API already accepts; no new auth scheme), Redis pub/sub for fan-out across API workers (Redis already runs; no new service).
- Per-item soft locks with heartbeats and expiry, presence (who is looking at which review item), paragraph-anchored discussion threads, and optimistic concurrency on resolutions (a version column) so two reviewers can never both resolve one item.
- Tables `review_locks`, `review_threads`, `review_comments`.
- Console: live queue updates in the review hub, avatars on items, lock badges, a thread panel.
- Gates: two-client race tests; lock expiry; fan-out across two worker processes; Caddy WebSocket pass-through (validate with the real Caddy binary as ARCH-46 L2 does).

**Facts proven by the ARCH-41–47 builds (do not rediscover them):**

- Capability registration is SIX places plus gates: `app/core/entitlements.py` (constant, `CAPABILITY_KEYS`, an `Entitlement(...)` entry — without it `has_capability` raises), `app/api/capability_gate` display names, `scripts/seed_quota_tiers.py` (ENTERPRISE_CAPABILITIES only for an Enterprise key — and make sure it is NOT in BUSINESS_CAPABILITIES), frontend `constants/capabilities.ts`, `constants/planFeatures.ts` — the typed label AND `PLAN_FEATURE_ORDER` — `verify_arch36.py GATED_PAGES` (and retire a `HELPER_EXEMPT` entry if you give that helper a caller: ARCH-47 retired `procurementCasePath`), and `verify_hardening_master.py` ENT (only). Then `python scripts/seed_quota_tiers.py --carry-forward`. On a fresh database seed the price book first (`scripts/seed_price_book.py`; `-SeedPriceBook`).
- Gating is imperative: `capability_gate.require_capability(...)` → 402 `CAPABILITY_REQUIRED`, the second line of every route after the workspace check. A WebSocket has no 402: check the capability during the handshake and close with a policy code (1008) before accepting; gate that too.
- Request bodies are validated BEFORE the route body runs: a 402 gate test must send a VALID body (ARCH-47's D5 hit a 422 before the 402 with an extra field).
- **The review hub now has nine kinds**: EXTRACTION, ASSERTION, ANOMALY, MERGE (42), SPLIT (43), TABLE (44), CORROBORATION (45), OBLIGATION (46), POSTING (47, reason POSTING_EXCEPTION). The newest hub migration is `arch47_step1_erp_posting`: `review_queue_view_v8()` = v7 + a POSTING arm, `REVIEW_KINDS`, `REVIEW_REASONS` (a module constant — ARCH-47 first forgot it and five earlier verifiers failed), `internal_after_47()`. Anything ARCH-48 adds to resolutions must keep every kind's `resolution._DISPATCH` entry; the version column for optimistic concurrency must be enforced at the ONE resolution entry point (`resolution.resolve_item`) so every kind gets it, and bulk resolution must report a stale version per item rather than fail the batch.
- New automation triggers (if any): events in `app/core/automation_events.py`, `TriggerSpec`s in `triggers.py` (22 triggers over 23 events after ARCH-47), rebuild `ck_outbox_events_visibility_vocabulary` from `internal_after_47()`, `verify_arch37` EMITTERS + `later` + count pins, and the count pins in `verify_arch38` B2, `verify_arch40` T1, `verify_arch41` check_catalog, `verify_arch43` T8/M16, `verify_arch44` W1, `verify_arch45` W1/MS28, `verify_arch46` W1/MS34, `verify_arch47` W1/MS32, and `scripts/automation_conformance.EXPECTED_TRIGGERS` (22). Flow Builder now has EIGHT actions (ARCH-47 added `erp.post`, registered as `ERP_ACTION_TYPES` beside `COMMERCIAL_ACTION_TYPES`; `verify_arch37` R1 accepts that set).
- New migrations insert BEFORE the contract step and re-parent it; widen head pins in `verify_arch31, 31_step0, 34–47`, the chains in `verify_arch40` A8, `verify_arch41..46` (their chain checks now accept arch47), `verify_arch47` T2 (it pins `STEP3 == arch47`: widen it) and `verify_hardening_master`, each with an `ARCH48-S1:` sentinel, never replacing an earlier one. `verify_arch47` A1 runs `apply_arch47.py --check`, which reports files a later milestone changed as "superseded" (its `LATER_MILESTONE` regex matches `ARCH48-S1:`), so keep a sentinel in every file you touch. `apply_arch37.py` / `apply_arch39.py` carry an explicit `SUPERSEDING_SENTINELS` tuple (ARCH-47 added `ARCH47-S1:`/`ARCH47-S2:`): add `ARCH48-S1:`/`ARCH48-S2:` if you edit a file those applies created (the flow catalog, `automationFlow.ts`, `ActionConfigForm.tsx`).
- **audit_logs is append-only** (a trigger refuses DELETE, so an organization with audit rows can never be deleted). A gate that must COMMIT data (real concurrency across connections) should patch the audit writer for its dedicated organization and delete that organization afterwards — `verify_arch47.concurrency_gate` shows how. A committed org that already wrote audit rows stays in the development database (the conformance script's orgs do too).
- **Real concurrency finds real bugs.** ARCH-47's `INSERT … ON CONFLICT (ledger key) DO NOTHING` still raised a UniqueViolation when two planners raced on its SECOND unique index (the idempotency key); the fix is `ON CONFLICT DO NOTHING` with no target. For locks and optimistic concurrency, test with N threads on N sessions behind a `threading.Barrier`, committed, not in one rolled-back session.
- **SQLAlchemy JSONB stores Python None as the JSON literal `null`** unless the column is `JSONB(none_as_null=True)`; a CHECK like `x IS NULL` then fails. ARCH-47's erasure found this.
- Work-queue claiming: `SELECT … FOR UPDATE SKIP LOCKED`, a lease token and a lease expiry, COMMIT before any I/O, record only if the lease is still yours (`erp.service._claim/_record`). Soft review locks should follow the same shape (heartbeat = extend the lease).
- Mock servers in-process: `scripts/erp_mock_targets.py` (HTTPS with a self-signed certificate the SSRF-safe client trusts via `test_ssl_context` + `allow_private_ranges`, both refused in production; an SFTP server with a pinned RSA key). For WebSockets use Starlette's `TestClient.websocket_connect` and, for cross-process fan-out, two uvicorn processes on localhost against the sandbox Redis.
- `work_items` has NO `organization_id`; composite keys `(id, workspace_id)` everywhere (`erp_postings (id, workspace_id)` and `erp_targets (id, workspace_id)` joined the list). PostgreSQL 16's `ON DELETE SET NULL (column)` clears one column of a composite FK.
- A new job type is registered in three places (`handlers/__init__.py` phase constant and `_HANDLERS`, a profile in `profiles.py`); a harness calling handlers must call `register_all()` first — including a harness that commits in its own sessions.
- One clock: ARCH-46's `obligations.service.now()` and ARCH-47's `erp.service.now()`; take ARCH-48's lock expiry and heartbeats from one injectable `now()` so gates can pin time.
- **XML is confined** (`scripts/verify_arch16.py` S1): lxml / `xml.*` may be imported only by `identity/saml_gateway.py` (defusedxml) and ARCH-47's hardened `erp/formats/xmlsafe.py` (`parse` for untrusted input refuses DOCTYPE/ENTITY; entities never resolved). Parse any new XML through `xmlsafe`. ARCH-47 first broke this gate and only the wider regression run caught it: run the whole earlier-verifier list (below), not just `--regression`.
- **Windows is a target platform, and the sandbox is not Windows.** ARCH-47's first Windows run found: `file:` URLs must go through `url2pathname` (`xsd.local_path`), never `url[7:]` (drive letters, `%20`); in-process mocks must be addressed by the literal address they bind (`127.0.0.1`), never `localhost` (Windows resolves it to `::1` first); `zipfile` stamps the OS into each entry (pin `create_system`); text written with `write_text` becomes CRLF. The SSRF-safe client now tries the next resolved address only while nothing was sent (`ConnectError.request_sent`); keep that distinction for WebSockets and Redis connections. Test path handling with spaces and `#`, and resolver order, on Linux the way `verify_arch47` P1/N3 do.
- **A mutation counts only if its gate passes unmutated** (`verify_arch47`: static gates are run unmutated first; database mutations check their D-gate passed). Keep that rule, and make later gates report a missing prerequisite by name rather than raising a KeyError.
- Console dates: instants via `utils/displayTime.formatTimestamp`, calendar dates via `formatCalendarDate`; `new Date(x).toLocale*String(` fails `scripts/verify_arch30_tranche3.py` H8. `curly: all` is an eslint error (no brace-less `if`).
- `requirements.txt` is UTF-16LE with CRLF (git stores it as binary). ARCH-47's apply engine keeps UTF-16 files as they are.
- Mutation harnesses: build the anchor BEFORE the refusal check; patch the binding the code uses; rebuilt modules get the ORIGINAL exception classes and dataclasses (`_share_exceptions`); text-reading gates mutate a COPY of the file (`_with_file`); a gate's own error path must not raise for an unrelated reason (ARCH-47's MS42 was first "caught" by a `relative_to` ValueError in the gate's message formatting — read every `caught_by`). A live run can stop after the step a mutation targets (`live_e2e(patches, until=...)`).
- Known pre-existing failures (reproduce on the baseline before worrying): `verify_arch38` A1 (`apply_arch38.py` refuses `preset_service.py`, edited by ARCH-42); `scripts/verify_arch30_tranche1.py` 30T1-G4; `scripts/verify_arch21.py` G8 and G16; `verify_hardening_master --db` D1/D5/D6/D7 and `verify_hardening_final --db` "Payment loop (Stripe)" without real gateway secrets; `scripts/verify_arch10_step3.py` / `step9.py`. **Never run `verify_arch10_step3.py` against a database you back up.**

**Sandbox (the build environment, not the product):** a Python 3.12 venv; PostgreSQL 16 may not survive between tool calls — start it at the top of every call that needs it; build pgvector 0.8.1 from source if Ubuntu's is 0.6; install `backend/requirements.txt` (convert from UTF-16) without torch/paddle/sentence-transformers; Hugging Face downloads are refused (403), GitHub releases work (a Caddy 2.10.2 binary validates the Caddyfile; `verify_arch46` L2 uses `$CADDY`); `npm ci --ignore-scripts` in the foreground; a `git worktree` of the baseline gives a clean tree; `.gitattributes` pins `eol=lf`, so prove CRLF by rewriting a clean copy's text files. Long runs (the full certification with `--regression`) go in the background with the log polled.

**How to work:**
1. Clone, then audit the real code before writing anything. Correct anything in this prompt or the roadmap that the code contradicts, and say what you corrected.
2. Build in the clone; sentinels `ARCH48-S1:` (backend) / `ARCH48-S2:` (console).
3. Deliver `backend/apply_arch48.py` (generated from `git diff <baseline>` like `apply_arch47.py`: sha-guarded `new` and `edit` operations, atomic, `--check`/`--rollback`/`--list`), `backend/verify_arch48.py` (offline; `--db` end to end including HTTP 402/200 and the WebSocket handshake refusal; `--mutate` where every mutation must be caught for the RIGHT reason; `--build`; `--regression` → `verify_arch47.py --db`), `run_arch48.ps1`, `ARCH-48-FINAL-CERTIFICATION.md`, and the ARCH-49 handoff prompt.
4. Prove apply idempotency and rollback on clean LF and CRLF trees of the baseline, and run `verify_arch48.py --db --mutate --build --regression` before certifying. Report only what actually passed, and say plainly what could not be run in the sandbox.
