[pytest]
testpaths = tests
python_files = test_*.py
env =
    ENVIRONMENT=test
    RATE_LIMIT_ENABLED=False
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration_s3: marks tests as S3 integration tests requiring a live endpoint